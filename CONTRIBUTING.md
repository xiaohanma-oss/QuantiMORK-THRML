# Contributing

1. Fork and create a feature branch
2. `pip install -e ".[dev]"` and `git submodule update --init`
3. Run `pytest` before submitting
4. Update README.md if you change module structure or functionality
